// Shared non-secret build settings.
// Local secrets are optional so the project still builds before configuration.

TRANSIT_PROXY_BASE_URL =
TRANSIT_PROXY_CLIENT_TOKEN =

#include? "Secrets.xcconfig"
