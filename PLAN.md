# GoHome 개발 계획

## 1. 제품 목표

GoHome은 앱을 열었을 때 사용자의 현재 위치를 기준으로 가까운 서울 지하철역을 찾고, 해당 역에 접근하는 열차와 막차 정보를 빠르게 보여주는 iOS 앱이다.

첫 단계에서는 정식 출시보다 개발자 본인의 실제 사용 경험을 우선한다. 일상에서 반복 사용하면서 데이터 정확도, 화면 구성, 갱신 주기와 장애 상황을 검증하고, 완성도가 충분해졌을 때 유료 개발자 등록·중계 서버·Android·전국화를 검토한다.

## 2. 확정된 원칙

- 초기 비용은 0원으로 유지한다.
- iOS 17 이상을 먼저 지원한다.
- 대상 지역은 서울 및 서울시 실시간 API 지원 구간으로 한정한다.
- 앱이 전면에 있을 때만 위치와 실시간 데이터를 갱신한다.
- 위치 권한은 `When In Use`만 요청한다.
- 가까운 역은 처음부터 한 곳으로 단정하지 않고 상위 후보를 보여줄 수 있게 설계한다.
- 열차 위치는 GPS 좌표가 아닌 현재 역·진입·도착·출발 상태임을 UI에서 명확히 한다.
- 실시간 정보에는 데이터 기준시각과 지연 상태를 함께 표시한다.
- 앱은 Cloudflare Worker HTTPS 중계를 호출하고 서울시 원본 키를 앱 번들에 포함하지 않는다.
- 서울시 실시간 지하철 API가 HTTP만 제공하므로 Worker에서 제한된 원본 API만 호출한다.
- API 키와 개인 설정은 Git에 커밋하지 않는다.

## 3. MVP 범위

### 포함

- 앱 사용 중 위치 권한 요청과 거부 상태 처리
- 현재 위치 기준 가까운 역 상위 3개 계산
- 사용자가 역 후보를 직접 변경하는 기능
- 역·호선·방향별 실시간 도착정보
- 열차의 현재 역과 운행 상태 표시
- 방향·종착역별 막차 시간 조회
- 평일·토요일·일요일/공휴일 구분
- 새로고침, 마지막 갱신시각, 오래된 데이터 표시
- 네트워크 장애 시 오류 메시지와 재시도
- 역 수동 검색 fallback

### 제외

- 회원가입과 로그인
- 사용자 서버 데이터베이스
- 백그라운드 상시 위치 추적
- 푸시 알림과 위젯
- 경로 검색과 환승 경로 추천
- 혼잡도
- Android
- 전국 도시철도 통합
- 앱스토어·TestFlight 정식 배포
- 열차의 실제 선로상 GPS 좌표 제공

## 4. 비용과 배포 전략

### 현재 단계: 0원

- Xcode와 SwiftUI 사용
- 무료 Apple Account의 Personal Team으로 개인 iPhone 테스트
- 서울 열린데이터광장·공공데이터포털 무료 API 사용
- 역 정적 데이터를 앱 번들에 포함
- Cloudflare Workers Free 플랜과 기본 `workers.dev` 주소 사용
- 별도 유료 서버·도메인·데이터베이스 미사용

무료 Personal Team은 개인 기기 테스트 용도다. App ID·등록 기기·프로비저닝 프로필이 7일 후 만료될 수 있어 주기적으로 Xcode에서 다시 설치해야 한다. 기기 등록은 플랫폼당 최대 3대이며, 무료 상태에서 지인에게 TestFlight 링크나 설치 파일을 원격 배포할 수는 없다.

### 비용 전환 조건

다음 중 하나가 발생하기 전까지 유료 전환하지 않는다.

- Xcode 연결 없이 지인에게 지속 배포해야 함
- TestFlight가 필요함
- Worker 무료 호출 한도나 서울시 API 호출 한도가 실제 문제가 됨
- 사용자 수 증가로 더 긴 공용 캐시나 별도 저장소가 필요함
- 앱스토어 출시를 결정함

전환 시 우선순위는 Apple Developer Program 등록, 무료 Cloudflare Worker 중계, 필요 시 유료 Worker 순서다.

## 5. 데이터 소스

| 용도 | 데이터 | 사용 시점 |
|---|---|---|
| 역 좌표·노선·운영기관 | 국가철도공단 도시광역철도 역사정보 | Phase 1 |
| 역명과 역코드 매핑 | 서울교통공사 역명으로 지하철역 검색 | Phase 1 |
| 역별 실시간 도착 | 서울시 지하철 실시간 도착정보 | Phase 2 |
| 노선별 열차 상태 | 서울시 지하철 실시간 열차 위치정보 | Phase 3 |
| 막차 | 서울교통공사 역코드로 막차 시간표 검색 | Phase 4 |
| 전체 예정 시간표 | 서울교통공사 역코드로 열차 시간표 검색 | fallback |
| 공휴일 | 한국천문연구원 특일 정보 | Phase 4 |
| 전국 확장 기반 | 국토교통부 TAGO 지하철정보 | 장기 |

국토교통부 교통 CCTV API는 제품 요구사항과 관련이 없어 사용하지 않는다.

## 6. 기술 구조

```text
GoHome App
├── App                 앱 진입점
├── Features            화면과 ViewModel
├── Domain              앱 내부 모델
├── Data
│   ├── API             공공 API 구현체
│   └── Repositories    역 정적 데이터
├── Core
│   ├── Configuration   API 키·환경 설정
│   ├── Location        Core Location
│   └── Networking      교체 가능한 API 인터페이스
└── Resources           Info.plist·역 좌표·Assets
```

UI는 공공 API URL을 직접 알지 않는다. `TransitAPIClient` 프로토콜만 사용하고, 현재 구현체는 동일한 프로토콜 뒤에서 Cloudflare Worker HTTPS 중계를 호출한다.

## 7. 개발 단계

Phase 0–3의 코드·구성 구현 항목은 완료했다. 실제 실행 증명이 필요한 승인 관문은 전체 Xcode와
iPhone을 사용할 수 있을 때 확인하며, 통과 전에는 해당 Phase를 최종 100% 완료로 표시하지
않는다. Phase 0–2 세부 검증표는 [`Docs/PHASE_0_2_ACCEPTANCE.md`](Docs/PHASE_0_2_ACCEPTANCE.md),
Phase 3 검증표는 [`Docs/PHASE_3_ACCEPTANCE.md`](Docs/PHASE_3_ACCEPTANCE.md)에 정리한다.

Phase 0–2의 코드·구성 구현 항목은 31/31로 완료했다. 다만 실제 실행 증명이 필요한 아래 세
승인 관문은 전체 Xcode와 iPhone을 사용할 수 있을 때 확인하며, 통과 전에는 Phase 0–2 전체를
최종 100% 완료로 표시하지 않는다.

1. Simulator 빌드와 `GoHomeTests` 전체 통과
2. Personal Team 실제 iPhone 실행 및 정확·대략적·거부·지원 범위 밖 위치 시나리오 확인
3. 실제 역 전광판과 앱의 방향·열차 순서 비교

세부 검증표는 [`Docs/PHASE_0_2_ACCEPTANCE.md`](Docs/PHASE_0_2_ACCEPTANCE.md)에 정리한다.

### Phase 0 — 저장소와 실행 골격

- [x] Git 저장소 초기화
- [x] SwiftUI 앱과 테스트 타깃 생성
- [x] 위치 권한 설명 추가
- [x] 비밀키용 `xcconfig`와 `.gitignore` 구성
- [x] 가까운 역 계산 골격 구성
- [x] 실시간 도착 API 클라이언트 골격 구성
- [ ] 전체 Xcode 설치 후 Simulator 빌드
- [ ] Personal Team 선택 후 실제 iPhone 실행
- [x] GitHub 원격 저장소 연결

완료 조건: 앱이 iPhone에서 실행되고 위치 권한 상태와 가까운 역 후보가 표시된다.

### Phase 1 — 역 데이터 기반 완성

- [x] 국가철도공단 역사정보 원본 확보
- [x] 서울·지원 구간만 선별하는 변환 스크립트 작성
- [x] 역명·부역명·환승역 정규화 규칙 정의
- [x] 서울 API 역 ID·외부코드 매핑
- [x] 전체 역 JSON 생성 및 앱 번들 교체 (563개 물리 역·696개 노선별 역)
- [x] 거리 계산과 5km 범위 제한 테스트
- [x] 가까운 역 상위 3개 선택 UI
- [x] 위치 거부·축소 정확도·서울 밖 상태와 수동 역 검색 fallback 처리

완료 조건: 서울 지원 범위 어디서든 가까운 역 후보가 일관되게 계산된다.

### Phase 2 — 실시간 도착정보

- [x] Cloudflare Worker HTTPS 중계 코드와 자동 테스트 작성
- [x] iOS 클라이언트를 Worker URL·Bearer 토큰 방식으로 전환
- [x] Worker Free 플랜 배포 및 `/health` 확인
- [x] 배포된 Worker를 통한 실제 도착정보 확인 (2026-08-19 시청역 13건)
- [x] 실제 인증키로 응답 샘플 저장 (`tmp/api-samples/`, Git 제외)
- [x] JSON 디코딩 필드 검증 (2026-08-19 시청역 14건, `INFO-000`)
- [x] 동일 역명·복수 호선 필터링
- [x] 방향·종착역·급행·남은 시간 표시
- [x] `recptnDt` 기준 데이터 노후도 계산
- [x] 앱 전면에서 40초 간격 자동 갱신
- [x] 백그라운드 전환 시 자동 갱신 작업 취소
- [x] 수동·자동 갱신 중복 요청 및 40초 내 자동 재요청 방지
- [x] 앱 인증 설정·토큰·호출 한도·Worker·서울시 API 오류 분류
- [x] 갱신 실패 시 마지막 정상 데이터 유지 및 오래된 데이터 경고
- [x] 도착정보 상태·오류·갱신 제어 단위 테스트와 Worker 회귀 테스트 추가
- [x] 오류 정규화 Worker 재배포 및 종단간 재검증 (2026-08-19 시청역 12건)

완료 조건: 실제 역 전광판과 비교해 방향과 열차 순서가 신뢰 가능한 수준으로 표시된다.

### Phase 3 — 실시간 열차 위치

- [x] 인증이 필요한 Worker 고정 경로 `/v1/positions?line=` 구현·배포
- [x] 실제 2호선 응답 필드·`INFO-000`·상태 코드 종단간 검증
- [x] 가까운 역의 각 호선에 대해 열차 위치 요청
- [x] 열차 상태 코드를 진입·도착·출발·전역 출발로 변환
- [x] 숫자 ID 정렬이 아닌 명시적 19개 노선·28개 경로 정적 번들 생성
- [x] 분기·순환·종착역을 고려한 현재 역과 선택 역 사이의 남은 역 수 계산
- [x] 방향·종착역·일반/급행/특급·막차 표시
- [x] 선로상 GPS 좌표가 아닌 현재 역 기반 정보임을 안내
- [x] Home 화면에 간단한 노선 스트립과 열차 위치 영역 구현
- [x] 도착·위치정보를 하나의 전면 자동 갱신 주기와 중복 방지 상태로 통합
- [x] 위치 오류 시 노선별 마지막 정상 데이터·기준시각·오래된 상태 유지
- [x] Worker 회귀 테스트·정적 데이터 검증·Swift 위치 테스트 추가
- [ ] 전체 Xcode에서 Simulator 빌드와 `GoHomeTests` 전체 통과
- [ ] 실제 iPhone에서 전면/백그라운드 갱신과 위치 UI 확인
- [ ] 실제 역 전광판과 현재 역·방향·남은 역 수 비교

완료 조건: 사용자가 자신이 탈 열차가 몇 역 전인지 이해할 수 있다.

### Phase 4 — 막차

- [ ] 막차 API 실제 응답과 역코드 검증
- [ ] 평일·토요일·일요일/공휴일 선택
- [ ] 한국천문연구원 공휴일 월 단위 캐시
- [ ] 24시·25시 형식의 영업일 시간 파서
- [ ] 방향·종착역별 막차 목록
- [ ] 현재 시각 기준 남은 시간 계산
- [ ] 실시간 막차 열차와 예정 시간표를 구분

완료 조건: 주요 환승역에서 공식 시간표와 비교 테스트를 통과한다.

### Phase 5 — 개인 실사용 안정화

- [ ] 1~2주 일상 사용 기록
- [ ] 위치 오탐·역명 매핑 실패 기록
- [ ] API 오류와 응답 지연 기록
- [ ] 배터리 사용량 점검
- [ ] 접근성 글자 크기와 VoiceOver 점검
- [ ] 개인정보 처리 문구 초안
- [ ] 앱 아이콘과 최소 디자인 정리

완료 조건: 개발자 본인이 일상에서 기존 지하철 앱 대신 사용할 수 있다.

### Phase 6 — 제한적 지인 테스트

- [ ] 무료 Personal Team의 7일 재설치 제약을 테스트 참여자에게 안내
- [ ] 최대 3대 기기에서 개발 설치 검증
- [ ] 피드백 템플릿 작성
- [ ] 크래시 재현 정보를 수동 수집
- [ ] 유료 개발자 등록 필요성을 다시 판단

완료 조건: 소수 지인의 반복 피드백으로 핵심 UX 가설을 검증한다.

## 8. 품질 기준

- 앱은 API 키가 없어도 실행되어야 한다.
- 위치 권한 거부 시 수동 역 선택 경로가 있어야 한다.
- 네트워크 오류가 앱 크래시로 이어지지 않아야 한다.
- 실시간 응답의 기준시각을 숨기지 않는다.
- 오래된 데이터는 실시간처럼 표시하지 않는다.
- 역 정적 데이터와 API 역코드 연결은 자동 테스트한다.
- 자정 전후 시간 계산을 단위 테스트한다.
- API 원본 필드명을 View까지 전달하지 않는다.

## 9. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| 서울시 API 호출 한도 | 전면 화면에서만 호출, 30~45초 간격, 동일 요청 중복 방지 |
| 서울시 원본 키 노출 | Worker secret에만 저장하고 앱에는 개인용 Bearer 토큰만 포함 |
| 서울시 실시간 API의 HTTP 전용 제공 | 키·응답 보호를 위해 무료 HTTPS 중계 서버를 우선 적용하고 직접 호출은 제한 |
| 역명 불일치 | 내부 고유 ID와 별칭 테이블 운영 |
| 서울 밖 구간 미제공 | 지원 범위를 화면에 명시하고 예정 시간표로 fallback |
| GPS 오차 | 거리 상위 3개와 수동 선택 제공 |
| 실제 열차 좌표 오해 | 현재 역 기반 상태·추정임을 명시 |
| 무료 서명 7일 만료 | 개인 테스트 단계에서 재설치 감수, 배포 필요 시 유료 전환 판단 |

## 10. 다음 작업

1. 전체 Xcode와 iPhone을 사용할 수 있는 시점에 Phase 0–3 승인 검증표를 실행한다.
2. 실제 역 전광판과 앱의 방향·현재 역·남은 역 수를 비교하고 불일치 사례를 기록한다.
3. 승인 관문 통과 후 Phase 4 막차 API와 영업일 시간 처리를 구현한다.
